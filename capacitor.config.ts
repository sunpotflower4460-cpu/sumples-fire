import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sunpotflower.sumplesfire',
  appName: 'Sumples Fire',
  webDir: 'dist'
};

export default config;
